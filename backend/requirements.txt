fastapi==0.141.1
uvicorn[standard]==0.52.1
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2
# PyJWT instead of python-jose: jose pulls in legacy ecdsa/rsa/pyasn1
# dependencies for algorithms this app never uses (only HS256 — see
# settings.jwt_algorithm), and two of those transitive deps had advisories
# with no available fix. PyJWT has no such baggage.
PyJWT==2.13.0
cryptography==49.0.0
bcrypt==4.1.2
python-multipart==0.0.32
aiofiles==23.2.1
aiosmtplib==5.1.2
# Real runtime dependency, not just a test tool — src/integrations/meta.py
# uses this to call the Meta Marketing API. (Also used in tests, which is
# why it's easy to mistake for a test-only package — see requirements-dev.txt.)
httpx==0.28.1
pandas==2.1.4
numpy==1.26.3
python-dateutil==2.9.0.post0
slowapi==0.1.10
apscheduler==3.11.3
